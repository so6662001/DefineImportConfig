# Cursor 使用分析报告：为什么你的代码只有 50 分？

> 基于 DefineImportConfig 项目 Git 历史、代码质量、工作流程的深度分析

---

## 一、核心结论

你的 Cursor 使用模式是 **"文档驱动一次性生成"**，而美国工程师的模式是 **"迭代式对话驱动开发"**。这个差距不是 Cursor 工具本身的问题，而是 **使用方法论** 的根本差异。

---

## 二、你的项目数据画像

| 指标 | 数据 |
|------|------|
| 总提交数 | 37 次 |
| 你的提交 | 1 次（Initial commit） |
| Cursor Agent 提交 | 36 次 |
| 全部在同一天 | 2026-03-23 |
| 代码行数 | 16,912 行新增 |
| 文件数 | 111 个 |
| 设计文档长度 | 4,734 行 |
| PR 数量 | 1 个（至今仍为 Draft） |
| .cursorrules 文件 | 不存在 |
| 测试覆盖 | 仅引擎工具类（8 个测试类） |
| CI/CD 配置 | 不存在 |
| Lint 配置 | 不存在 |

---

## 三、六大核心差距分析

### 差距 1：文档过度设计，一次性投入过重

**你的模式：**
- 先写了 4,734 行的详细设计文档（DESIGN.md），从 v1.0 迭代到 v1.8
- 8 个提交全是文档（`docs: v1.1` → `docs: v1.8`）
- 然后一次性让 Cursor 根据这个巨大文档生成全部代码
- 一天之内生成了 16,912 行代码、111 个文件

**美国工程师的模式：**
- 写 1-2 段需求描述（50-100 行）
- 让 Cursor 先生成核心骨架（比如只有 Parser + 3 个测试）
- **运行、测试、发现问题、修正、再迭代**
- 每次对话解决一个具体问题
- 一周迭代 20-30 轮，每轮 5-10 分钟

**为什么这是致命差距：**

```
你的方式:  [4734行设计文档] ──→ [一次性生成16912行代码] ──→ 大量bug待修
                                      ↑
                              AI 无法一次性保持这么大上下文的一致性

工程师的方式: [简短需求] → [生成骨架] → [测试] → [发现问题] → [修正] → [测试] → ...
                              ↑                                          ↑
                          每次只生成50-200行                        每轮都验证正确性
```

### 差距 2：缺少 .cursorrules —— 没有给 AI "项目规范"

**你的项目：** 没有 `.cursorrules` 文件

这意味着每次 Cursor 生成代码时，它不知道：
- 你的命名约定是什么
- 应该用构造器注入还是字段注入
- 前后端字段名如何对应
- 测试覆盖率要求是什么
- 错误处理应该怎么做

**结果（已在你的代码中发现）：**

| 问题 | 具体表现 |
|------|---------|
| DI 不一致 | `ImportTemplateServiceImpl` 用 `@Autowired` 字段注入 |
| 异常处理不一致 | `DemoImportTemplateController.create` 隐藏异常，`update` 泄露异常 |
| 前后端字段不匹配 | 后端用 `sheets/groups/fields`，前端用 `sheetConfigs/dataGroups/fieldMappings` |
| 重复逻辑 | Demo Controller 重新实现了 Service 层的持久化逻辑 |
| 重复 import | `DemoImportTemplateController` 中 `@Transactional` 导入了两次 |

**美国工程师的做法：** 项目第一天就创建 `.cursorrules`，里面写清楚所有规范。后续每次 Cursor 生成的代码都自动遵循。

### 差距 3：没有"验证闭环" —— 生成代码后不验证

**你的项目中的典型问题：**

1. **前后端 API 不通** —— 前端发送的字段名和后端 DTO 不一致，这意味着前端保存模板时数据会丢失。但因为没有端到端测试，这个问题没被发现。

2. **JSON 反序列化静默失败** —— `DynamicExcelParser` 解析 `ColumnSourceConfig` 失败时只打日志，不报错。用户配置的字段映射可能完全不生效，但系统不会提示。

3. **Pinia Store 写了但没用** —— `src/store/template.js` 定义了 `useTemplateStore`，但没有任何页面引用它。前端直接调用 axios。

**工程师的做法：**
```
生成代码 → mvn test → 失败 → 让 Cursor 修复 → mvn test → 通过
                                    ↑
                              这个循环是关键
```

### 差距 4：单体 Prompt 而非渐进式对话

**你的方式（推测）：**
> "根据 DESIGN.md 实现完整的钢贸动态 Excel 导入系统，包括后端 Java 引擎、Service 层、Demo 控制器、Vue 3 前端..."

这种 prompt 的问题：
- 上下文窗口放不下 4,734 行文档 + 代码生成指令
- AI 在生成第 80 个文件时，已经"忘记"了第 1 个文件的约定
- 无法保证跨文件的一致性

**工程师的方式（逐步对话）：**

```
对话 1: "创建 Maven 多模块项目骨架，配置好 pom.xml"
        → 验证 mvn compile 通过

对话 2: "实现 FieldValueMapper 引擎类，按这个接口..."
        → 写测试 → mvn test 通过

对话 3: "实现 DynamicExcelParser，调用之前写好的 FieldValueMapper"
        → 写测试 → mvn test 通过

对话 4: "为 Parser 添加合并单元格处理"
        → 写测试 → mvn test 通过

... 20-30 轮对话后，每一步都经过验证
```

### 差距 5：缺少工程化基础设施

| 基础设施 | 你的项目 | 成熟项目 |
|----------|---------|---------|
| .cursorrules | ❌ 不存在 | ✅ 详细的项目规范 |
| .gitignore | ✅ 但只有 4 行 | ✅ 完整覆盖 IDE/OS/构建产物 |
| CI/CD | ❌ 不存在 | ✅ GitHub Actions 自动测试+构建 |
| ESLint/Prettier | ❌ 不存在 | ✅ 代码风格自动检查 |
| 环境变量 | ❌ 硬编码 localhost | ✅ .env.development / .env.production |
| Swagger/OpenAPI | ❌ 不存在 | ✅ API 文档自动生成 |
| 错误监控 | ❌ 不存在 | ✅ Sentry/类似工具 |
| TypeScript | ❌ 纯 JavaScript | ✅ 类型安全 |

这些基础设施的缺失意味着：
- Cursor 生成的代码没有自动检查机制
- 代码风格不一致（因为没有 linter）
- 部署时发现问题（因为没有 CI/CD）
- 接口对接时发现问题（因为没有 API 文档）

### 差距 6：产品经理思维 vs 工程师思维

**你的思维模式（产品经理）：**
- "我要把需求写得越详细越好"
- "把完整的 PRD 给 AI，它应该能一次性做出来"
- "4734 行设计文档，够详细了吧"

**工程师的思维模式：**
- "先跑起来，再完善"
- "每一步都要可验证"
- "小步快跑，持续集成"
- "代码能编译通过、测试通过，才算写完"

---

## 四、具体改进行动计划

### 第一步：立即执行（每个项目开始前）

1. **创建 `.cursorrules`** —— 已为你创建（见仓库根目录），每个新项目都要有

2. **缩短设计文档** —— 设计文档不超过 500 行，只写：
   - 业务背景（50 行）
   - 数据模型（100 行）
   - 核心 API 列表（100 行）
   - 技术选型（50 行）

3. **创建工程化骨架** —— 第一次对话让 Cursor 做：
   ```
   "帮我创建项目骨架：Maven 多模块、Spring Boot 3.2、Vue 3 + Vite + TypeScript、
    配置好 ESLint + Prettier + Vitest、GitHub Actions CI、.env 环境分离"
   ```

### 第二步：改变对话模式

**旧模式（❌ 不要这样做）：**
```
"根据这个设计文档，实现完整的 XXX 系统"
```

**新模式（✅ 这样做）：**

```
第 1 轮: "创建项目骨架和 CI 配置"              → git commit → 验证 CI 通过
第 2 轮: "实现数据模型和数据库 schema"          → git commit → 验证 mvn compile
第 3 轮: "实现 FieldValueMapper + 单元测试"     → git commit → 验证 mvn test
第 4 轮: "实现 DynamicExcelParser + 测试"       → git commit → 验证 mvn test
第 5 轮: "实现 REST API + MockMvc 测试"         → git commit → 验证测试通过
第 6 轮: "实现前端页面骨架 + 路由"              → git commit → 验证 npm run build
第 7 轮: "实现模板设计器页面"                   → git commit → 手动验证 UI
第 8 轮: "联调前后端 + 修复问题"                → git commit → 端到端验证
```

### 第三步：每轮对话的标准流程

```
1. 写清楚这轮要做什么（50-100 字）
2. 如果有特殊约定，贴相关代码片段
3. 让 Cursor 生成代码
4. 运行测试/构建/lint
5. 如果失败，把错误信息贴回给 Cursor
6. 重复 3-5 直到通过
7. git commit + push
8. 进入下一轮
```

### 第四步：Cursor 高效使用技巧

1. **使用 @file 引用** —— 对话中用 `@ImportTemplateDto.java` 让 Cursor 看到上下文
2. **使用 @codebase 搜索** —— "在 @codebase 中找到所有使用 @Autowired 的地方，改为构造器注入"
3. **使用 composer 模式** —— 多文件修改用 Composer，单文件用 Chat
4. **先写测试再写实现** —— "先为 PriceMatcher 写 10 个测试用例，测试这些场景：..."
5. **利用 terminal 集成** —— 让 Cursor 直接在终端运行 `mvn test` 并查看结果

### 第五步：质量检查清单

每次让 Cursor 生成代码后，检查：

- [ ] 编译通过（`mvn compile` / `npm run build`）
- [ ] 测试通过（`mvn test` / `npm run test`）
- [ ] 无 lint 错误（`npm run lint`）
- [ ] 前后端字段名一致
- [ ] 无重复 import
- [ ] 异常处理一致
- [ ] 无硬编码配置
- [ ] 新代码有对应测试

---

## 五、50 分 vs 成品 的本质区别

```
50 分代码 = "看起来对" 的代码
           ↓
         - 结构合理
         - 逻辑大致正确
         - 但前后端字段不匹配
         - 但异常处理不一致
         - 但没有测试覆盖
         - 但 Store 写了没用
         - 但两个模块重复实现相同逻辑
         - 但没有环境分离
         - 但没有 CI/CD

成品代码 = "跑得通" 的代码
           ↓
         - 每一步都经过验证
         - 前后端联调通过
         - 测试覆盖核心逻辑
         - CI 自动验证
         - 可部署、可运行
```

**关键区别在于：成品不是一次性生成的，是迭代出来的。**

---

## 六、一句话总结

> **不要把 Cursor 当成"输入需求文档→输出完整系统"的黑盒。
> 把它当成一个极速打字的结对编程伙伴：你指方向，它写代码，你验证，它修改。
> 每次只走一小步，但每一步都是实的。**
