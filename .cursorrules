# DefineImportConfig 项目 Cursor Rules
# 本文件指导 Cursor AI 生成符合项目规范的高质量代码

## 项目概述
- 钢贸动态 Excel 导入系统
- 技术栈: Java 17 + Spring Boot 3.2 + MyBatis-Plus + Vue 3 + Vite + Element Plus
- 多模块 Maven 项目: define-import-core(引擎库) + define-import-demo(演示应用) + define-import-frontend(前端)

## 架构约束
- define-import-core 是独立 JAR 库，不依赖具体 Web 框架实现
- Demo 控制器必须委托给 core 的 Service 层，禁止在 Controller 中重复实现持久化逻辑
- 前端通过 /api 前缀代理到后端，生产环境需通过环境变量配置 API 地址

## Java 代码规范
- 使用构造器注入（@RequiredArgsConstructor），禁止字段级 @Autowired
- Service 方法必须有 @Transactional(rollbackFor = Exception.class) 注解
- 禁止吞没异常（catch + ignore），必须记录日志或向上抛出
- JSON 反序列化失败必须返回明确错误，不能静默跳过
- 使用 Lombok @Data/@Builder 减少样板代码
- 统一使用 SLF4J 记录日志
- Controller 层禁止暴露原始异常信息给客户端
- 文件上传必须校验: 扩展名白名单 + 大小限制 + 非空检查

## Vue 前端规范
- 单个 .vue 文件不超过 400 行，超出则拆分为子组件
- 所有 API 调用通过 src/api/ 模块统一管理
- 使用 Pinia store 管理跨页面共享状态，页面级状态使用 composable
- 统一使用 axios 拦截器处理错误和 loading 状态
- 必须处理 API 错误并给出用户友好的提示
- 表单提交前做客户端校验
- 使用 TypeScript 类型定义后端 DTO 对应的接口

## 前后端字段映射（关键！）
- 后端 DTO 字段名和前端请求字段名必须一致
- SheetConfigDto -> sheets (不是 sheetConfigs)
- GroupConfigDto -> groups (不是 dataGroups)
- FieldMappingDto -> fields (不是 fieldMappings)
- 新增字段时同步更新前后端

## 测试要求
- 每个 engine 类必须有对应的单元测试
- Service 层需要集成测试
- Controller 层需要 MockMvc 测试
- 前端关键组件需要 Vitest 测试
- 测试覆盖率目标: 核心引擎 > 80%

## 文档规范
- DESIGN.md 只保留架构设计和核心流程
- API 文档使用 Swagger/OpenAPI 自动生成
- 变更日志使用 CHANGELOG.md

## 提交规范
- feat: 新功能
- fix: 修复
- refactor: 重构
- test: 测试
- docs: 文档
- 每次提交只做一件事，提交信息清晰描述变更内容
